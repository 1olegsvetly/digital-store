<?php
/**
 * payment_config.example.php — Пример конфигурации системы оплаты
 * ShillCMS Payment Module v2.7.0
 *
 * Скопируйте этот файл как payment.php и заполните свои данные.
 * НЕ добавляйте payment.php в публичный репозиторий!
 */

return [

    // ─── Включённые методы оплаты ─────────────────────────────────────────
    'methods' => [
        'yoomoney' => ['enabled' => true],
        'crypto'   => ['enabled' => true],
        'demo'     => ['enabled' => false],  // Демо-режим (без списания)
    ],

    // ─── YooMoney ─────────────────────────────────────────────────────────
    'yoomoney' => [
        // Номер кошелька YooMoney (получателя)
        'wallet'              => '4100XXXXXXXXXX',

        // Секрет HTTP-уведомлений (из настроек YooMoney → Безопасность → HTTP-уведомления)
        'notification_secret' => 'ВАШ_СЕКРЕТ_УВЕДОМЛЕНИЙ',

        // Тип оплаты: AC = банковская карта, PC = кошелёк YooMoney
        'payment_type'        => 'AC',

        // URL возврата после успешной оплаты (оставьте пустым для автоматического)
        'success_url'         => '',

        // URL возврата после неудачной оплаты (оставьте пустым для автоматического)
        'fail_url'            => '',
    ],

    // ─── Криптовалюта ─────────────────────────────────────────────────────
    'crypto' => [
        // Дополнительные заметки (не используется в коде)
        'notes' => 'Настройте кошельки в config/crypto_tokens.json',
    ],

];
